# sivaraman
#include <stdio.h>
#include<string.h>
#include<conio.h>
int main()
{
char str[100];
int temp;
printf("\nEnter the string",str);
scanf("%s",str);
char delims[] = " ";
char *result;
if {
    temp=strlen(result);
    if(temp % 2==0)
    {
        result = strtok(str, delims );
    }
    else
    {
        printf("\n ",str[0]);
    }
}
getch();
}
